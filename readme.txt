elements-all tags
margin-outside space of an elements
padding-inner space ofthe elements
border-box model: width and height properties include padding and border in the calculation of the element'
content box model: only the content of the element is included in the calculation, no padding or border
absolute tags-<a>,<img> points to url of the file
relative tags-./ , path to local file
block vs inline- block ele cover space from the left to right and inline ele only cover space as bounded by the tags
../    ,path to parent directory
hypertext- stores data like text,img,links,etc

use .classname to create  a class inbuilt
